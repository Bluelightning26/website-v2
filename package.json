{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "astro": "^5.15.5",
    "sass-embedded": "^1.93.3"
  }
}
